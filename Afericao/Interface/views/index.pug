extends layout

block content
  .w3-card-4
    header.w3-container.w3-teal
      h1 EMD App
    .w3-card-4
      w3-container
        table.w3-table-all 
          tr
            th Nome 
            th Resultado 
          
          each item in list
              tr 
                td #{item.nome.primeiro + ' ' + item.nome.último}
                td #{item.resultado}
                td 
                  a(href="/emd/" + item._id) EMD
